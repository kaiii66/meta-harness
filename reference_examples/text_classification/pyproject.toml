[project]
name = "text-classification"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "datasets",
    "litellm<1.82.7",
    "openai-harmony",
    "openai",
    "smolagents",
    "tenacity>=8",
    "tqdm",
    "weave",
]

[tool.uv]
package = false

[tool.ruff]
exclude = ["agents"]
